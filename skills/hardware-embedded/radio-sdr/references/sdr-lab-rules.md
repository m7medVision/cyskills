# SDR Lab Rules

1. RX only by default  
2. Transmission only in a shielded room + with an authorization document number  
3. Avoid protected bands such as aviation/emergency  
4. Do not publish unredacted frequency/location combinations in evidence (if sensitive)  
